name: dreamwell-dev

services:
  dreamwell:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev-base
    init: true
    working_dir: /app
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - dreamwell-cargo-registry:/usr/local/cargo/registry
      - dreamwell-cargo-git:/usr/local/cargo/git
      - dreamwell-target:/app/target
      - dreamwell-data:/app/data
      - dreamwell-frontend-dist:/app/crates/frontend/dist
      - dreamwell-trunk-cache:/root/.cache/trunk
    environment:
      CARGO_HOME: /usr/local/cargo
      DREAMWELL_STATIC_DIR: crates/frontend/dist
      DREAMWELL_DATABASE_URL: sqlite:/app/data/dreamwell.db
    entrypoint: ["/app/scripts/dev-run.sh"]
    develop:
      watch:
        - action: rebuild
          path: Dockerfile
        - action: rebuild
          path: docker-compose.dev.yml
        - action: restart
          path: ./scripts/dev-run.sh

volumes:
  dreamwell-cargo-registry:
  dreamwell-cargo-git:
  dreamwell-target:
  dreamwell-data:
  dreamwell-frontend-dist:
  dreamwell-trunk-cache:
