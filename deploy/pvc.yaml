apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dreamwell-data
  namespace: dreamwell
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
